import { HighlightDay4Directive } from './highlight-day4.directive';

describe('HighlightDay4Directive', () => {
  it('should create an instance', () => {
    const directive = new HighlightDay4Directive();
    expect(directive).toBeTruthy();
  });
});
